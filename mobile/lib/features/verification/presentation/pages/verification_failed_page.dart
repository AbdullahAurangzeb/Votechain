import 'package:flutter/material.dart';

/// Route target when [VerificationStatus.rejected] is returned after authentication.
///
/// Stitch UI for this screen is not implemented yet — routing only.
class VerificationFailedPage extends StatelessWidget {
  const VerificationFailedPage({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
